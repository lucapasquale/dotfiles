- name: Add elixir plugin
  command:
    cmd: ~/.asdf/bin/asdf plugin-add elixir
    creates: ~/.asdf/installs/elixir

- name: Install elixir
  command:
    cmd: ~/.asdf/bin/asdf install elixir latest && ~/.asdf/bin/asdf global elixir latest
